# Cargo configuration for WASM builds

[target.wasm32-unknown-unknown]
# Enable getrandom support for WASM
rustflags = [
    "--cfg", "getrandom_backend=\"wasm_js\""
]

[build]
# Default target for wasm-pack builds
# target = "wasm32-unknown-unknown"
